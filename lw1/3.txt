ѕараметр charset указывает кодировку символов документа.

≈сли charset не указан - в HTTP 1.1 говоритс€, что по умолчанию будет использован charset ISO-8859-1.

≈сли charset указан неверно то отобразитс€ нечитаемый текст.

http://localhost/lw1/all_begin_from_http.html отдаетс€ нечитаемый текст т.к. файл находитс€ в 
кодировке utf-8, чтобы текст был читаемым в файле all_begin_from_http.html необходимо задать 
тег <meta charset="utf-8"/>
